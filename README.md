# customer-churn-analyser
This project implements a production-oriented machine learning pipeline for predicting customer churn. It transforms raw customer data into actionable predictions using a structured workflow that includes data ingestion, preprocessing, model training, evaluation, and deployment via an interactive web application.
